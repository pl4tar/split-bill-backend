env: "local"
# storage_path: ""
http_server:
  address: "localhost"
  port: "8090"
  timeout: 4s
  idle_timeout: 60s

database:
  host: ${DB_HOST}
  port: ${DB_PORT}
  username: ${DB_USERNAME} 
  password: ${DB_PASSWORD}
  database: ${DB_NAME}
  sslmode: "disable"